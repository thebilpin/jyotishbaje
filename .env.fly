APP_NAME="Astroway"
APP_ENV=production
APP_KEY=base64:8hH4aqQFExlhcwdFtXHHivHXG7S3NU2HGU7T5qqWRSs=
APP_DEBUG=false
APP_URL=https://jyotishbaje.fly.dev

LOG_CHANNEL=stderr
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=info

# Database Configuration (set these as Fly.io secrets)
DB_CONNECTION=mysql
DB_HOST=${DATABASE_HOST}
DB_PORT=${DATABASE_PORT:-3306}
DB_DATABASE=${DATABASE_NAME}
DB_USERNAME=${DATABASE_USERNAME}
DB_PASSWORD=${DATABASE_PASSWORD}
DATABASE_URL=${DATABASE_URL}

BROADCAST_DRIVER=log
CACHE_DRIVER=${CACHE_DRIVER:-file}
FILESYSTEM_DISK=local
QUEUE_CONNECTION=database
SESSION_DRIVER=${SESSION_DRIVER:-file}
SESSION_LIFETIME=1440

MEMCACHED_HOST=127.0.0.1

# Redis Configuration (optional)
REDIS_URL=${REDIS_URL}
REDIS_HOST=${REDIS_HOST:-127.0.0.1}
REDIS_PASSWORD=${REDIS_PASSWORD}
REDIS_PORT=${REDIS_PORT:-6379}

MAIL_MAILER=smtp
MAIL_HOST=${MAIL_HOST}
MAIL_PORT=${MAIL_PORT:-587}
MAIL_USERNAME=${MAIL_USERNAME}
MAIL_PASSWORD=${MAIL_PASSWORD}
MAIL_ENCRYPTION=tls
MAIL_FROM_ADDRESS=${MAIL_FROM_ADDRESS}
MAIL_FROM_NAME="${APP_NAME}"

AWS_ACCESS_KEY_ID=${AWS_ACCESS_KEY_ID}
AWS_SECRET_ACCESS_KEY=${AWS_SECRET_ACCESS_KEY}
AWS_DEFAULT_REGION=${AWS_DEFAULT_REGION}
AWS_BUCKET=${AWS_BUCKET}
AWS_USE_PATH_STYLE_ENDPOINT=false

PUSHER_APP_ID=${PUSHER_APP_ID}
PUSHER_APP_KEY=${PUSHER_APP_KEY}
PUSHER_APP_SECRET=${PUSHER_APP_SECRET}
PUSHER_HOST=${PUSHER_HOST}
PUSHER_PORT=443
PUSHER_SCHEME=https
PUSHER_APP_CLUSTER=mt1

VITE_PUSHER_APP_KEY="${PUSHER_APP_KEY}"
VITE_PUSHER_HOST="${PUSHER_HOST}"
VITE_PUSHER_PORT="${PUSHER_PORT}"
VITE_PUSHER_SCHEME="${PUSHER_SCHEME}"
VITE_PUSHER_APP_CLUSTER="${PUSHER_APP_CLUSTER}"

JWT_SECRET=TnrWcw2zK5lE0nOD7bE3HIJaRMyP8sYPU0SvYAtw6tIbagoFjFjVrw7uhRr2w2mX
FCM_SERVER_KEY=${FCM_SERVER_KEY}
FIREBASE_PROJECT_ID=${FIREBASE_PROJECT_ID}

# Payment Gateway Configuration (set as Fly.io secrets)
RAZORPAY_KEY=${RAZORPAY_KEY}
RAZORPAY_SECRET=${RAZORPAY_SECRET}
STRIPE_KEY=${STRIPE_KEY}
STRIPE_SECRET=${STRIPE_SECRET}
PAYPAL_CLIENT_ID=${PAYPAL_CLIENT_ID}
PAYPAL_SECRET=${PAYPAL_SECRET}

# Agora Video Calling
AGORA_APP_ID=${AGORA_APP_ID}
AGORA_APP_CERTIFICATE=${AGORA_APP_CERTIFICATE}

# Fly.io Optimization Settings
FLY_HTTP_TIMEOUT=30
FLY_DB_TIMEOUT=30
FLY_API_TIMEOUT=15
FLY_CACHE_VIEWS=true
FLY_CACHE_CONFIG=true
FLY_CACHE_ROUTES=true
FLY_MAX_EXECUTION_TIME=300
FLY_MEMORY_LIMIT=512M